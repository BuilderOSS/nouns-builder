#import "./ClankerTokenCard.graphql"

fragment ClankerToken on ClankerToken {
  ...ClankerTokenCard
  pairedToken
  poolId
  poolHook
  createdAt
  transactionHash
  msgSender
  dao {
    id
    name
    contractImage
  }
}
