#import "./ClankerToken.graphql"

fragment ClankerTokenWithHolders on ClankerToken {
  ...ClankerToken
  holders(first: 10, orderBy: balance, orderDirection: desc) {
    id
    holder
    balance
    updatedAt
  }
}
