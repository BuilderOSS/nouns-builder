query daoMultisigs($daoId: String!, $creators: [Bytes!]!) {
  daoMultisigUpdates(
    where: {
      dao: $daoId
      deleted: false
      creator_in: $creators
    }
  ) {
    ...DaoMultisigUpdate
  }
}

