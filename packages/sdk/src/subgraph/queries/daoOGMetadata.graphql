query daoOGMetadata($tokenAddress: ID!) {
  dao(id: $tokenAddress) {
    name
    description
    links {
      id
      key
      url
    }
    contractImage
    totalSupply
    ownerCount
    proposalCount
    tokenAddress
    metadataAddress
    auctionAddress
    treasuryAddress
    governorAddress
  }
}
