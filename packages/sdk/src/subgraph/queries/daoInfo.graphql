query daoInfo($tokenAddress: ID!) {
  dao(id: $tokenAddress) {
    name
    description
    contractImage
    projectURI
    links {
      id
      key
      url
    }
    totalSupply
    ownerCount
  }
}
