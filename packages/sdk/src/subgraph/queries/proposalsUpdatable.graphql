query proposalsUpdatable($where: Proposal_filter, $first: Int!, $skip: Int) {
  proposals(
    where: $where
    first: $first
    skip: $skip
    orderBy: timeCreated
    orderDirection: desc
  ) {
    ...ProposalUpdatable
    votes {
      ...ProposalVote
    }
  }
}
